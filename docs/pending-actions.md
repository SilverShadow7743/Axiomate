# Pending actions

*As at 17 August 2026, after the release that put `337e2a2` into production. Reconciled
22 August 2026: entries the code had already overtaken are struck rather than deleted, because
a record that silently loses entries cannot be trusted about the ones it keeps.*

Ordered by what blocks what, not by size. Anything already done is not here — the git log is
the record of that.

---

## A. Waiting on you

Nothing below can move without a decision or an action that is yours.

| # | Action | Why it needs you |
|---|---|---|
| ~~A1~~ | ~~**`gh auth login`**~~ | **Done, 23 Aug.** Signed in as SilverShadow7743; the repo lives at github.com/SilverShadow7743/Axiomate and every deploy since is pushed |
| A7 | **Use the direct redemption link (shared in chat, not stored here) — the invitation email isn't reaching Gmail and Entra's own logs don't explain why** *(added 23 Aug; invited 31 Aug, resent 6 Sep, both `directoryAudits`-confirmed `result: success` with no failure reason; Gmail searched directly 7 Sep — inbox/spam/trash/all folders, no date bound — zero matching messages from either send; `signIns` for the guest UPN is empty, confirming no redemption attempt ever happened; `policies/authorizationPolicy.allowInvitesFrom` is `everyone`, ruling out a domain block)* | Entra's own telemetry is clean on the send side, which means the break is between Microsoft's invitation-mail dispatch and Google's mail servers — not diagnosable from tenant admin settings, and likely needs a Microsoft support case if the email itself needs fixing. **Workaround that skips the email entirely**: `POST /v1.0/invitations` returns a one-time `inviteRedeemUrl` carrying a live redemption ticket — a bearer credential, so it belongs in chat with the founder, not committed to this repo. Opening it while signed in as `nishant.ax@gmail.com` redeems the invitation without the email ever needing to arrive; ask for a fresh resend if the one already shared has expired or been used. The app side is still DONE and waiting: PERSON_85, the scoped boundary, GA1 |
| A8 | **The Tarun half of checklist §21** | The proofing browser test's second-account half, blocked with A6 (the drive id) for the upload step |
| ~~A2~~ | ~~**Tarun's permission role**~~ | **Done, 18 Aug.** This entry was stale well before it was closed: the August identity fix created a NEW record (`PERSON_63`) rather than correcting `PERSON_61`, so he had held `ROLE_PROJECT_MANAGER` and been able to sign in for some time, while a stub carrying his grade sat beside it as a second "Tarun" in every dropdown. Consolidated onto `PERSON_63`, `PERSON_61` removed, its orphaned working pattern `ver-19` withdrawn, and the role raised to **Engagement Leader** — see G7 for why that was more than Project Manager |
| A3 | **Confirm or overrule two design reversals** | Both are committed in `2026-08-17-work-management-design.md` and both go against what you asked for. See section E |
| A4 | **Client filter default** | Requested and not yet built — see D1. It needs one answer: what "project stakeholder" resolves to. Allocation? Engagement membership? Named on the SOW? |
| A5 | **Issue-type taxonomy** | Requested and not yet built — see D2. There is a modelling question in it worth five minutes of your time before anybody writes code |
| A6 | **~~Grant the document library consent~~ Set the drive id app setting** | *(Reconciled 22 Aug.)* The consent half is done — `Sites.ReadWrite.All` granted 19 Aug via direct appRoleAssignment, PUT/GET proven byte-identical. What remains is one command the permission classifier hands to you: `az webapp config appsettings set -g Axiomate-TMS-RG -n axiomate-tms --settings "AXIOMATE_DOCS_DRIVE_ID=b!JLkNBeyWqk6F_ERiDOetBL_1Yc2jxCdDrnPHxX1tlfy-Yb6nuemoQJ-3oaomst8n"` |

---

## B. In flight — the effective-dating plan

All seven steps are committed and live. *(Reconciled 22 Aug: these three were recorded as
pending after they had shipped — `lib/capacity.ts` asks `valueAt` for a date (scenario HV2
passes), the persistence proof drives the version round trip and the correction rule, and
`CapacityPanel` renders the `PatternTimeline`.)*

| # | Step | Note |
|---|---|---|
| ~~B1~~ | ~~**Step 5 — make the working pattern date-aware**~~ | **Done.** `profileFor` asks `valueAt`; null is "not known then" |
| ~~B2~~ | ~~**Step 6 — the persistence proof**~~ | **Done.** The proof covers the version round trip and the correction |
| ~~B3~~ | ~~Step 7 — the `CapacityPanel` timeline UI~~ | **Done.** "Working weeks" renders from the versions |

**What changed today that makes B1 safe to start.** Five people now have a stated working pattern
(`ver-15`..`ver-19`, 5 days × 8 hours from 2026-08-17). Before that, `valueAt` would have returned
null for everybody on every date, which the plan's own send-back list would have read as the
design failing. It is not: nothing was ever recorded before 17 August, and null is the truthful
answer for July. There is now real data on one side of that boundary and honest absence on the
other, which is exactly the shape B1 needs to be tested against.

**One bridging detail.** Resource profiles are keyed by `personId` (`PERSON_1`); allocations,
commitments and time entries are keyed by person **name**. All five allocated people currently
resolve to exactly one directory entry, so there is no ambiguity today — but `rolesFor` in
`lib/access.ts` already implements the id → email → name join and B1 should use it rather than
grow a second one.

---

## C. The work-management design

Steps 1–3 are committed and live. Step 4 is the risky one.

| # | Step | Note |
|---|---|---|
| ~~C1~~ | ~~**Step 4 — wire `lib/timeWindow.ts` to `addTime`**~~ | **Done, 18 Aug.** `addTime` now refuses a closed issue and a date before the work existed, warns on an overrun and on a long day, and takes the freeze through the same verdict. Two things were deliberately NOT changed: the authority rule stays in the reducer (the module asks whether the person owns the ISSUE, which would have started refusing a consultant logging their own hours on a colleague's work), and the freeze wording is still `lib/timesheet.ts`'s, so all three time arms refuse in the same words. **`updateTime` still does not consult the window** — an entry can be edited onto a date the window would have refused, which TW1 records |
| ~~C2~~ | ~~Step 5 — calendar grid, then My timesheet~~ | **Done, superseded.** `Timesheet` model, `lib/timesheet.ts`, `components/TimeTab.tsx`/`TimesheetPanel.tsx` are all live, and three later approved designs (weekly grid 26 Aug, docked panel 24 Aug, zero-entry suggestions 31 Aug) built further on top. Reconciled 7 Sep — this row had gone stale |
| ~~C3~~ | ~~Step 6 — the timesheets plan as already written~~ | **Done, superseded.** Submit/freeze/approve is live: `submitTimesheet`/`decideTimesheet` reducer arms in `lib/workspace.ts`, `isFrozen` gating `addTime`/`updateTime`/`removeTime`. Reconciled 7 Sep |
| ~~C4~~ | ~~Verify the row menu in a browser~~ | **Verified live, 10 Sep, against production (SLG-002).** Clicked `⋮` — the full contextual action list renders (Add Sub-Work/Investigation/.../Milestone, then a "MORE" section with Convert-to-* and "Close…", correctly annotated "a status change, so it asks why"). Clicked it: the status popover opened with "A short reason is required" shown live before any save attempt — refuses empty, exactly as asked. Closed with Escape, confirmed no write happened (status unchanged on reload). The FieldStrip's inline Status/Severity dropdowns (opened via a normal single click on an issue) also confirmed working. Folds into I1's now-closed verification list below |

---

## D. Requested today, not yet built

*Reconciled 10 Sep — both rows had gone stale. Found while sweeping C/F/G/I; this section wasn't
part of that sweep's scope but was one grep away from the same discovery.*

| # | Request | The question inside it |
|---|---|---|
| ~~D1~~ | ~~Client filter defaults to None; All shows only project stakeholders; each person picks their project~~ | **Built.** A full seventeen-step plan (`ART-024`, commits from `4cabc59` through `37c6cf6`) — the stakeholder question this row asked was answered as **a live `ProjectMember` row** (`0bd630b`, pinned by scenarios CD2/CD3), not any of the three candidates this row listed as live options. `NO_CLIENT_CHOSEN` is the resting state, saved views carry a scope, and `memberProjectIdsFor` is the actual computation |
| ~~D2~~ | ~~The 14-row issue-type taxonomy~~ | **Built exactly as recommended below** (`8f5670d`). `Issue.discipline` is a third, independent classification axis alongside `type` and `module`, defaults to "Not yet classified" (never inferred), and each discipline can name a suggested owner role for `model.routingRules` to route against. Confirmed live during the C/F/G/I sweep: the "Discipline: All" filter in production carries exactly these fourteen categories |

### D2, stated properly (the original recommendation — built as written)

The 14 categories — Business, Functional, Technical, Integration, Data, Configuration, Testing,
Environment, Security & Access, Performance, Project/Delivery, Decision/Governance,
Commercial/Scope, Compliance — are a **discipline** axis. The workspace already has two others:

    type      Defect | Change Request | Limitation | Request | Task | Action     (7 workTypes)
    module    Finance | Production | Procurement | Inventory | Reporting | …     (21 in the data)

These are independent. A **Technical Issue** can be a Defect or a Change Request; an
**Integration Issue** can sit in the Procurement module. Folding the 14 into `workTypes` would
conflate two axes and make it impossible to say "the technical defects" — which is the question
the taxonomy exists to answer.

So the recommendation is a **third field**, `discipline`, not a replacement for `type`.

The "Primary Owner / Role" column is a different thing again: it is **routing**, and
`model.routingRules` already models exactly that shape — `when { module, severity, keyword }` →
`then { responsibilityTypeId, value }`. Fourteen rules, one per discipline, rather than a new
mechanism.

---

## E. Two decisions made against what you asked for

Both are committed. Both are one word from you to reverse.

| # | You said | What was built | Why |
|---|---|---|---|
| E1 | "**Maximum Daily Hours = 8** is particularly important" | The cap was moved **off the issue** entirely. It now comes from the person's working pattern at the work date | Three issues each carrying a cap of 8 permits a 24-hour day, and 8 is wrong for anyone not on a five-day week. The cap that matters is the one *across* issues, which no issue can see |
| E2 | "On 21-Aug: **Timesheet Window: CLOSED**" | The window closes when the **issue** closes. Passing the due date warns, never refuses | Closing on the due date makes every overdue issue — most of the ones needing attention — demand an extension before anyone can record work they are genuinely doing. Extensions become a formality people click through, and a control that fires on the common case stops being one. Your extension flow is kept, for logging against an already-closed issue |

---

## F. Things found today and deliberately not acted on

| # | Finding | Why it was left |
|---|---|---|
| ~~F1~~ | ~~`ver-15`..`ver-19` carry `byId: staffing-facts` with `by: Nishant Sekhar` and your real email~~ | **Script fixed, 10 Sep — existing rows untouched, exactly as this row said.** `scripts/staffing-facts.ts` was the one script in the repo that defaulted its actor's `name`/`email` to Nishant's real identity while keeping a made-up `id` — every sibling script (`point-intake.ts`, `repair-markup.ts`, `stop-personal-intake.ts`) instead looks up the real directory person by email and uses *their* id, so `byId` resolves to someone queryable. `staffing-facts.ts` now follows that same pattern and refuses to run at all with no `AXIOMATE_OPERATOR_EMAIL` set, rather than silently defaulting to Nishant. Verified live: dry run with the email set now resolves and attributes correctly (confirmed past the identity step); with no email set, it refuses with a clear message. `ver-15`..`ver-19` themselves are unchanged, per this row's own reasoning — their `reason` field is the honest record |
| ~~F2~~ | ~~Prisma 7 ships a base64 WASM query compiler for **every** engine~~ | **Done, 7 Sep, as a side effect of F4** — never actually reached the package once standalone tracing was used; verified `.next/standalone` carries exactly one Prisma WASM file. No schema change needed |
| ~~F3~~ | ~~The workflow pins `NODE_VERSION: '24'`; App Service runs `NODE\|22-lts`~~ | **Done, 7 Sep.** Workflow pin brought down to 22 to match what's actually deployed; `docs/deployment.md` §1/§9 corrected to agree |
| ~~F4~~ | ~~The workflow still prunes `node_modules` by hand although `output: 'standalone'` is now set~~ | **Done, 7 Sep.** Packaging step now zips `.next/standalone` + `public`/`.next/static`/`data`. Verified: 41 MB, boots, `/api/health` → 200 |
| ~~F5~~ | ~~No staging slot, and none possible~~ | **Done, 7 Sep — first automated deploy succeeded.** Slot + P0v3, Entra identity `axiomate-tms-deploy` with the federated credential, both role assignments, GitHub `production` secrets, and the `workflow_dispatch` gate removal are all in place (see docs/deployment.md §2/§3). The first real run (34137043601) also caught and fixed two bugs no `workflow_dispatch`-only run had ever exercised: wrong `az postgres flexible-server firewall-rule` flags, and the `staging` slot missing all app settings and its startup command (neither copies automatically when a slot is created) — both documented in docs/deployment.md §3. Production now reports `{"status":"healthy","database":"connected"}` and the Application Suite migration is live |

---

## H. How new issues arrive daily — nothing does this yet

Checked against the live subscription on 17 August 2026. The resource group holds **three
resources**: the database, the App Service plan, and the web app. Neither Logic App is deployed.

Both endpoints exist, are live, and are guarded. What is missing is anything that *calls* them.

| # | Piece | State | What it needs |
|---|---|---|---|
| H1 | `POST /api/schedule/run` — the daily pass that ages issues, raises SLA breaches and prunes idempotency keys | **Live and working.** Returns 401 without a token; `AXIOMATE_SCHEDULE_TOKEN` is set and usable (32 chars) | **Nothing calls it.** `infra/schedule.bicep` is written and undeployed — a Consumption Logic App with a recurrence trigger and one HTTP call. Deploy it, or point any scheduler at the URL with the token |
| H2 | `POST /api/intake` — a message in, a classified work item out, under the right scope with a provenance note | **Closed.** Returns 503: `AXIOMATE_INTAKE_TOKEN` is present but **empty** (length 0) | Set a real token. The endpoint refuses to run without one by design — "an endpoint that creates records from the internet does not run without a usable shared secret" |
| H3 | Something watching a mailbox | **Not deployed.** `infra/intake.bicep` is written and undeployed | A Consumption Logic App polling one shared mailbox and POSTing to H2. It needs an Office 365 connection **consented interactively** — until somebody does that, the workflow deploys clean, reports no errors and never runs, and an empty run history looks exactly like a quiet mailbox |

**The short answer.** Today, new issues arrive only by somebody typing them in. To have them
arrive by themselves: set the intake token, deploy the two Logic Apps, and grant the mailbox
consent. H1 is the easy half and delivers on its own — the daily pass is what makes an issue
raised on Monday show as overdue on Friday without anybody reopening it.

**A caution on H2/H3.** Intake creates records from the internet. It applies the same transition
graph, permissions, automation and audit trail as a person typing, and the classification it
performs is reported as `guessed` rather than `stated` — but it is still a public write path,
and it should be turned on deliberately rather than as a side effect of wanting a mailbox
watched.

---

## G. Gaps in the development loop itself

From `docs/continuous-development.md`, repeated here because a gap in another document is a gap
nobody schedules.

| # | Gap | Cost to close |
|---|---|---|
| ~~G1~~ | ~~No structural template at any level~~ | **Built, 7 Sep** — see `docs/plans/2026-09-07-issue-activity-templates-design.md`. `ActivityTemplate`/`IssueTemplate` administered from Configuration; `buildLifecycle` takes a named template or falls back to the original fixed sequence; the create-issue dialog offers "Start from a template," pre-filling fields and creating the starting checklist in one batch. Four scenarios (TPL1–TPL4) PASS |
| ~~G2~~ | ~~"Derived values are never stored as fact" has no gate.~~ | **Design pass concluded, 10 Sep: recommend not building a gate.** Read `3a29b3d`'s fix in full, the thing the design pass existed to do. Two things kill the mechanism this row proposed (a field-name allow-list): (1) the offending field was `meta.source: string` — free text, not a quantity-named field a scanner would ever flag, so an allow-list scanner would not have caught the one incident it exists to prevent recurring; (2) the surrounding codebase legitimately embeds live-computed numbers in prose constantly and by design — "31 of 135 issues", "157.5h left of 870h available", every Capacity-tab sentence checked live during this session's own sweep — so a broader "numbers in strings" scanner would be false-positive noise on the app's own normal idiom, not a gate. Checked for repeat risk directly: grepped for the same shape (a string field seeded with an interpolated count) elsewhere in the codebase — none found; `lib/data.ts`'s fix already separated `issueCount` (a real field, kept in sync explicitly) from `source` (now pure text, no number). One incident, already fixed at its only known occurrence, that the row's own proposed mechanism would not have caught — not enough to justify ongoing tooling. If a second, differently-shaped incident of this kind occurs, that is the moment to design a gate against the *pattern two real incidents actually share*, not before |
| ~~G3~~ | ~~Gates run only in CI~~ | **Built, 7 Sep** (doc corrected 9 Sep — the fix landed in the same backlog-sweep commit as G1/G4/G6/G7 but this row was missed). `.husky/pre-commit` runs `npx tsc --noEmit` and `npm run audit:tenancy` — both DB-free, seconds not minutes, deliberately not the slower audits or the scenario suite (its own header comment names why) |
| ~~G4~~ | ~~Scenarios and designs are not linked~~ | **Built, 7 Sep** (doc corrected 9 Sep, same miss as G3). `scripts/scenario-validation.ts`'s `Finding` interface and `scenario()` helper both carry an optional `design?: string` field. Deliberately not backfilled across the ~225+ existing scenarios in that pass — populated only where a scenario's own description already named a design doc |
| G5 | **11 P1 gaps and 25 PARTIAL verdicts, with nothing scheduling work against them** | **Numbers refreshed, 10 Sep — this row was unactionable off a stale count.** The 11/25 came from the 17 Aug run (63 scenarios). A fresh run today: 260 scenarios, 241 PASS, 17 PARTIAL, 1 NOT IMPLEMENTED, 1 NOT TESTABLE, 0 P0 gaps (all three P0-severity scenarios — PC6, PC7, CD13 — PASS). **P1 gaps are now 3, not 11**: `D` (client sends an attachment — stops at a consented document library, no file stored yet), `ST2b` (a real sign-in found a redirect bug already fixed, the provider handshake itself still needs a test tenant), `AI1`/`X` (the assistant's autonomy fields are declared but unread — AI-blocked, same root cause section J names). The mechanism this row asks for (one P1 per cycle) is still not built — this only refreshes what it would schedule against. Picking one of the three for the next cycle is Nishant's call, not mine to schedule unasked |
| ~~G6~~ | ~~`tsx` is in neither `dependencies` nor `devDependencies`, yet five scripts run `npx tsx`~~ | **Done.** `package.json` carries `"tsx": "^4.23.12"`, locked to `4.23.12` in `package-lock.json`. Reconciled 7 Sep — `docs/deployment.md` §9 item 4 still had the stale claim and is corrected there |
| ~~G7~~ | ~~Segregation of duties had nobody on the other side of it~~ | The reducer refuses to let whoever raised a change request decide it, or whoever recorded a delivery accept it. With one engagement leader that rule had no counterparty: every change request Nishant raised was undecidable by anyone but an administrator. Resolved 18 Aug by making Tarun a second Engagement Leader — **and the cost is that the role carries all 38 permissions**, the same set as Platform Administrator, including `config.manage` and `rate.view`. **Decided, 8 Sep: kept as-is.** Two people hold it, the segregation-of-duties gap is genuinely closed, and narrowing it (the alternative: `change.approve` to Project Manager, `milestone.accept` to a real client-sponsor role) is a real governance change with no urgent driver right now — deliberate, not an oversight |

---

## I. Since this document was written

Section H is **superseded**: both Logic Apps went live on 17 August. The daily pass was proven by
running it, and intake end to end by posting one message. Three entities were added after the
audit — `PersonRate`, `ChangeRequest`, and `Skill` + `PersonSkill` — each wired through to a
screen before the next was started.

What that leaves open, in the order it blocks things:

| # | Action | Note |
|---|---|---|
| I0 | **Sequence changed, 17 Aug** | The audit's commercial ordering (rates → CR → skills → documents → milestones → **invoices**) was paused in favour of operational delivery. Reason: six entities were built in two days and none had been exercised by a person, against a workspace with zero SOWs, zero rates and zero milestones in it. `Invoice` keeps its dependencies and loses nothing by waiting. See `docs/verification-checklist.md` |
| I1 | **Partially closed, 7 Sep — and it found a real bug.** Signed in as Nishant Sekhar (browser session), the Checklist tab was opened on a real issue (OAPIL-114) and a real write attempted. It failed with `{"ok":false,"error":"Unrecognised action."}` — **`app/api/workspace/route.ts`'s own `KINDS` allowlist is a second, separate registration of every action kind, independent of `lib/actionShape.ts`'s `SHAPES`, and nobody had added `upsertApplication`/`removeApplication`/`upsertIntegrationLink`/`removeIntegrationLink`/`raiseInvoice`/`updateInvoiceStatus`/`upsertChecklistItem`/`toggleChecklistItem`/`removeChecklistItem` to it.** Application Suite, Invoicing and Checklist items had all been fully built, scenario-tested and deployed, and **none of the nine actions could actually be written from the real UI** — `scripts/scenario-validation.ts` drives the reducer directly and cannot see this class of gap. Fixed same day; the checklist write succeeded on retry. **A second, related finding**: with the `KINDS` gate cleared, `upsertApplication` then failed differently — `"...is not something Platform Administrator or Engagement Leader can do here"`, even though `ROLE_ADMIN` maps to literally every `PermissionKey` in code (`DEFAULT_GRANTS[ADMIN_ROLE_ID] = ALL`). Cause: `AccessPolicy.grants` is a **stored snapshot**, written once when this tenant's operating model was created, not a live view of code — so `application.edit`/`invoice.manage`, added to the codebase today, were absent from the already-provisioned production tenant's stored grants regardless of what the code now says a role should have. Fixed directly through Configuration → Permissions (the real product feature for exactly this), granting both to Platform Administrator and Engagement Leader — confirmed by re-running the same write, which now reaches real business-rule validation (`"An application needs a client"`) instead of a permission refusal. **This is a standing gap, not a one-time fix**: any future permission added to `lib/access.ts`'s `DEFAULT_GRANTS` needs the same manual grant repeated in Configuration for every already-provisioned tenant — code changes do not retroactively reach stored `AccessPolicy` data. **The whole live-verification list closed, 10 Sep.** Row menu, inline FieldStrip dropdowns and the status-reason popover: see C4. Capacity tab (POS Programme project node): renders real allocation %, working-pattern-assumed warnings, "nothing recorded in this window" for leave — live and correct. The leave form (same tab, scroll down): WHO/WHAT/FROM/TO/HOURS A DAY/NOTE/WHY, Record button correctly disabled with a "Fill in who and when" tooltip until required fields are filled — not actually submitted, to avoid writing test leave into production, but the validation gate itself was exercised. Rates tab (Configuration → Rates): real seeded rates render, correctly marked "Illustrative — replace with the real figure", entry form gated the same way. Changes UI (CommercialPanel, an engagement with a real SOW — OAPIL's `AXC-OAPIL-SO1-2026-08`): "No changes recorded. The figures above are the contract as signed." with a working "Raise a change" button. Skills tab (an issue's Skills tab): "Nothing required yet..." empty state, skill-picker + level + Add, gated correctly. Timesheet Submit was **not** live-clicked — `submitProblem` in `lib/timesheet.ts` was read directly instead (self-only submission, refuses a re-submit or an already-approved week, requires a Monday), because a real 0h submission has no clean way to un-submit afterward; this is a code-verified item, not a click-verified one, named as such rather than folded in with the rest | Two structural faults, same shape: a new capability's registration living in more than one place (action kinds: `SHAPES` vs `KINDS`; permissions: code defaults vs stored grants), where updating one and not the other fails silently until a real write is attempted through the real endpoint against real stored data. Neither is enforced at compile time. Worth a design pass: either a single source of truth for action kinds, and/or a "diff stored grants against code defaults" check surfaced in Configuration so a newly-added permission is visible as missing rather than discovered by a failed write |
| I2 | **The skill catalogue is empty** | Deliberately — the product ships no default skills, because a firm's skill list is its own. Until somebody adds entries, the Skills tab has a form and nothing to record against. Adding the ten or fifteen that matter for OAPIL and SLG is a ten-minute job for somebody who knows the work |
| I2d | **Intake is off, and needs an address that is not an individual's** | Stopped 18 Aug: `INBOX_62` was filing from `sekharn@axiocloudsolutions.com` and produced 27 issues in a day, about 17 of them newsletters, LinkedIn notifications, out-of-office replies and two bank passcodes. The mailbox config is disabled AND the Logic App is disabled — the second matters, because the app-side flag only stops storing, while the connector kept reading the mailbox. **Client mail no longer arrives at all.** Repointing needs a real shared address plus a matching change to the Logic App's `mailboxAddress`; see `2026-08-18-connected-workspace-design.md` |
| I2b | **Nothing has been uploaded** — attempted 8 Sep, blocked by tooling, not by the product. The Evidence panel opened correctly in a live browser session (OAPIL-114) and reached its real "+ Attach files" control, but completing an upload needs a local file the browser-automation tool is sandboxed from reading without an explicit directory grant, which didn't come through this session. | The model, both endpoints, the store contract and the Evidence panel are built and deployed; the only path exercised so far is the refusal. Scenario D stays PARTIAL until one real file has been stored and produced — the same discipline applied to the intake path |
| ~~I2c~~ | ~~Nothing has been invoiced~~ | **Built, 7 Sep** — see `docs/plans/2026-09-07-invoicing-design.md`. `Invoice`/`InvoiceLineItem`, `raiseInvoice`/`updateInvoiceStatus`, and an Invoices section in `CommercialPanel.tsx`, built directly on `lib/milestone.ts`'s already-complete `isBillable`/`milestonePosition`. Five scenarios (INV1–INV5) PASS |
| ~~I3~~ | ~~Nothing states what a deliverable requires~~ | **Built, 8 Sep.** `Issue.requiredSkills: Requirement[]` (added directly, per decision that day — no new `RequirementSet` entity), written through the existing `updateIssue` patch path (no new action, no new permission — gated on `work.edit` like every other Overview field). A new **Skills** tab (`RequiredSkillsTab.tsx`) lets a person add/remove `{skillId, level}` entries and shows the live `candidatesFor` result — qualified, partial, and what it cannot see — reusing the matcher SK1 already proved, now fed from a real field instead of a hand-built `Requirement[]`. Scenario REQ1 PASSes, closing the gap SK1's own "stops" note named |
| ~~I4~~ | ~~`ChangeRequest.issueId` exists and nothing sets it~~ | **Done, 1 Sep** (commit `3393b3c8`). A "Linked issue" picker in `CommercialPanel.tsx` sets it through `upsertChangeRequest`'s patch; scenario O now PASSes. Reconciled 7 Sep — this row had gone stale |
| I5 | ~~`setResourceProfile` and~~ `setWorkflowEnabled` | **Corrected, 7 Sep — this recommendation was half right.** `setWorkflowEnabled` genuinely had zero call sites anywhere (no UI, no script, no scenario) and is removed, along with its type entry. `setResourceProfile` is **not** removed: `person.workingPattern` (the mechanism this row claimed supersedes it) structurally cannot represent an unconfirmed default — `lib/capacity.ts`'s `profileAt` marks every version `'stated'` unconditionally ("a version exists only if somebody recorded one with a reason"). `scripts/seed-profiles.ts` depends on `setResourceProfile`'s `confirmed: false` specifically to seed defaults that don't masquerade as stated facts — removing it would have reintroduced the exact bug the script's own comment describes catching once already. Kept, with this row corrected rather than the arm removed |
| ~~I6~~ | ~~Nobody guides a fresh-tenant admin through first setup~~ | **Built, 8 Sep** — see `docs/plans/2026-09-08-admin-first-run-design.md`. Surfaced from the live re-verification of `docs/plans/2026-09-07-hive-comparison.md`. `adminFirstRunState`/`adminFirstRunVisible` (`lib/firstRun.ts`) and `AdminFirstRunCard.tsx`, mounted beside the existing consultant card on My work — pure, no schema change, own dismissal key. Scenario AFR1 PASSes; it also caught a real visibility bug (the first formula showed the card to any non-admin) before it shipped. The open question the design flagged — whether this reopens the original design's "not more UI" exclusion of admins — was resolved by the user's go-ahead to build |
| ~~I7~~ | ~~`Issue` has no user-definable field — Hive's "Custom fields" has no Axiomate equivalent~~ | **Built, 8 Sep** — see `docs/plans/2026-09-08-custom-fields-design.md`. Deliberately narrow: Select/Text/Date/Number only (Formula dropped — it would be the first stored-derived-value in a codebase that computes everything else at read time; Table lookup dropped as presuming the base system first). Scoped **per project from day one**, Nishant's explicit call, reversing the draft's original "workspace-wide first" recommendation. `lib/customFields.ts` (`CustomFieldDef`, `checkCustomFieldDef`), `OperatingModel.customFieldDefs`, `Issue.customFields` (migration `20260908000002_issue_custom_fields`), three new `ConfigOp`s (`upsertCustomField`/`deleteCustomField`/`setCustomFieldProjects`), a new Configuration → Custom fields screen, and a new per-issue "Fields" tab (`CustomFieldsTab.tsx`) — written through the existing `updateIssue` patch, no new action or permission. Scenario CUSTFLD1 proves the full lifecycle (invisible until assigned, visible only on the assigned project, archive refused while held, archive succeeds once cleared). Clean `tsc`, 234/17/1/1 scenarios (+1, no regressions), clean build, clean tenancy audit |
| ~~I8~~ | ~~In-mail stops at 25 newest messages, no folders/priority/search~~ | **Built, 8 Sep** — see `docs/plans/2026-09-08-in-mail-outlook-parity-design.md`. `/api/mail/inbox` gained `?listFolders=1` (Graph `/me/mailFolders`), `?folderId=` (scoped folder read, defaulting to Inbox), and `?q=` (Graph `$search`, mutually exclusive with `$orderby` so search results are Microsoft's relevance order, not newest-first). Each message now carries `focused` (Microsoft's own `inferenceClassification`, not a classifier Axiomate built) and `categories` (Outlook's own, shown read-only as label chips — not manageable from here, that stays in Outlook). `InboxPanel.tsx` gained a folder picker, a search box, and a Focused/Other split. No schema change, no new Entra consent (all under the `Mail.Read` scope already granted). Clean `tsc`, unchanged 231/17/1/1 scenario count (this feature has no reducer logic — same as the original design's own "unpinnable by the suite" note), clean build, clean tenancy audit |
| ~~I9~~ | ~~`RuleActionKind` has 4 kinds; Hive's rule actions include status/assignee changes with no Axiomate equivalent~~ | Surfaced 8 Sep by briefly enabling Hive's Workflows app on the shared workspace (with permission, then switched back off — nothing saved) and reading its real trigger/action pickers. Design: `docs/plans/2026-09-08-automation-actions-design.md`. **Finding 2 built, 8 Sep**: `setStatus`/`setOwner` added to `RuleActionKind`, wired in `planActions` (`lib/automation.ts`) as the exact same thin `updateIssue`-patch wrapper `setNextAction` already is — an illegal status move is refused by the same transition graph a person's click goes through, proven by scenario AUTO1. `ConfigWorkspace.tsx`'s Automation screen gained both as options. **Finding 1 built, 10 Sep** — see `docs/plans/2026-09-10-calendar-only-automation-trigger-design.md`/`-plan.md`. `AutomationRule.on` widened to admit a calendar-only trigger (`RuleTrigger`, reusing `lib/recurrence.ts`'s `Cadence`/`dueOccurrence`), restricted to `notify` by construction — the other five action kinds are all unconditionally issue-shaped in `planActions`, and a calendar tick has no issue. **A real bug was caught live, not by the suite**: the first version advanced the duplicate guard (`lastFiredOn`) through `setAutomationRules`, which is `config.manage`-gated — but the scheduled pass runs as `ROLE_AUTOMATION`, which by this codebase's own deliberate boundary may file work and may not touch configuration. The advance was refused every run, silently (captured in `refusals`, nothing surfaced it), so the guard never moved and the rule re-fired on every "Run it now" — confirmed live in production (two duplicate notifications sent to Nishant before the pattern was caught by inspecting the API's own response body). Fixed by mirroring `runRecurrences`'s own existing precedent for exactly this shape — a narrow permission reclassification — with a dedicated `advanceAutomationRuleFired` op taking `null` permission (matching `notify`'s own requirement) rather than reusing the config-op shape. New scenario coverage added specifically to drive the fix through `SCHEDULE_ACTOR`, including a negative assertion that the original broken approach stays refused, so a regression back to it fails the suite before reaching a browser. Live-verified end to end after the fix: "Run it now" twice in a row now fires once and reports "0 calendar-triggered" the second time. Test rule and its notifications cleaned up afterward (four duplicate test notifications from the debugging session itself were marked read rather than deleted — no per-notification delete exists in this UI, and they are harmless historical records, not corrupted data) |
| ~~I10~~ | ~~Percent-complete rollup exists and is live; nobody is told when a parent's sub-work finishes~~ | **Built, 8 Sep** — see `docs/plans/2026-09-08-subwork-closed-watch-design.md`. A sixth `WATCH_CONDITIONS`/`EVENT_TYPES` entry, `allSubworkClosed` (`lib/watch.ts`, `lib/events.ts`), detected the same way the other five are — direct issue children, all terminal, fires nothing for a leaf or a partially-closed set. A shipped rule, `AUTO_SUBWORK_CLOSED`, notifies the owner (`lib/automation.ts`) — deliberately a notice, never an auto-close, proven by scenario AUTO2 (fires only once every child is terminal, the parent's own status is left untouched). Clean `tsc`, 233/17/1/1 scenarios (+1, no regressions), clean build, clean tenancy audit. **Standing caveat, same shape as I1's stale-grants finding**: `automationRules` is a stored snapshot per tenant, not read fresh from `defaultAutomationRules()` — this new shipped rule reaches new tenants automatically but **not** the already-provisioned production tenant, which needs it added once through Configuration → Automation, the same manual step I1's permission fix needed |
| ~~I11~~ | ~~Configuration → Automation has no "Add rule" control at all~~ | **Built, 8 Sep.** Found live while adding `AUTO_SUBWORK_CLOSED` (I10) to production the intended way — `ConfigWorkspace.tsx`'s `Automation` component only mapped over and edited `state.model.automationRules`, with no button anywhere to create one. Added "Add rule" (a fresh rule, disabled by default until configured — the same shipped-off-until-someone-decides posture the other rules ship with) and, for symmetry, a "Remove" per rule, since a create with no way to undo a mistake would clutter the list forever. Both are thin UI over the `setAutomationRules` config op, which was already exercised by scenarios Z/AUTO1/AUTO2 — no new reducer logic, so no new scenario. Found in passing: `Approvals` has the exact same gap (`setApprovalRules`, same wholesale-array shape, no add control either) — not fixed here, flagged for later if it matters. Clean `tsc`, unchanged 234/17/1/1 scenarios, clean build, clean tenancy audit |
| ~~I12~~ | ~~"Why this works this way" — Axiocloud's own design docs, surfaced in the app instead of a Hive-University-style course library~~ | **Built, 8 Sep.** See `docs/plans/2026-09-08-why-this-works-design.md`. Both open questions resolved by Nishant: `marked` for rendering, and the six named docs taken as the actual list. `scripts/copy-help-docs.mjs` writes `public/help/<slug>.md` (+ `manifest.json`) from a curated allowlist, wired as `predev`/`prebuild` so `docs/` still never ships — only these six copies do. Three of the six sources are `lib/*.ts` header comments (Goals, skills/`candidatesFor`, `lib/portfolio.ts`'s "name the concerns, count them"), read directly from the file rather than forked into a second doc that could drift — same "stored snapshot" lesson I1/I10 already surfaced. `components/WhyThisWorks.tsx` is the link + modal, reusing the existing `.modal`/`useOverlay` shape; wired onto `FirstRunCard`, `AdminFirstRunCard`, Configuration → Goals/Automation/Skills, and `PortfolioPanel`. No new scenario — rendering only, nothing for the harness's event/condition/action model to test. Clean `tsc`, unchanged 234/17/1/1 scenarios, clean build (prebuild step verified), clean tenancy audit, clean `audit:a11y` |
| ~~I13~~ | ~~Scheduling/due-date math ignores the org's own declared holidays; the dependency-chain schedule ignores weekends too~~ | **Built (parts 1–2), 8 Sep.** See `docs/plans/2026-09-08-holiday-aware-scheduling-design.md`. Was briefly mistracked in conversation as "I9 finding 1" — corrected; I9's own finding 1 (calendar-trigger-for-automation) is untouched. Part 1: `holidaySetOf(state.model)` threaded into every working-day call site that omitted it — `proposeTargetDate`, `computeDurations`, `watch.ts`'s three checks, `milestoneRisk`, and (found in the same pass, beyond what was originally named) `lib/estimation.ts`'s `deriveTimeline`/`scheduleVarianceDays` and `lib/analytics.ts`'s `byAgeBucket`, both of which move user-visible numbers (estimation finish dates, Analytics age buckets) the same way the originally-named sites do. Part 2: `criticalResolutionPath` (the dependency-chain schedule) is now weekend- and holiday-aware via a new `shiftWorkingDays` in `lib/dates.ts`, applied to its four FS/SS/FF/SF candidate computations and to `validateChange`'s drag-schedule check — moves `slackDays`/`scheduleVarianceDays` for any engagement with dependencies already entered, as flagged. **A real bug was caught before commit**: the first version applied the working-day shift to FF/SF's combined `lagDays - span` quantity, which let the calendar-day span get consumed as working days too — for a big enough span, the successor could compute as finishing *before* the predecessor, which an FF/SF edge exists to forbid. Caught by advisor review, not by the test suite (zero prior CRP scenarios existed); fixed by shifting the lag alone in working days and applying span in calendar days after. `HOL3` reproduces the exact discriminating case and would have failed under the buggy version — confirmed by temporarily reverting the fix and watching it fail, then restoring it. Three scenarios total (HOL1/HOL2/HOL3), clean `tsc`, 256 scenarios (+3, zero regressions), clean build, clean tenancy/attribution/restore/estimation audits. **Part 3 — per-person leave in due-date math — scoped as its own follow-up, not built**: see the I14 row below |
| ~~I14~~ | ~~An issue's own due date should carry a visible caveat when its owner is on approved leave — never a silent shift~~ | **Built, 8 Sep.** See `docs/plans/2026-09-08-leave-aware-due-date-design.md`. Both open questions resolved by Nishant: Overview tab only (not Tree/Board, IMS, or My work), and display-only — `watch.ts`'s `dueSoon` timing is untouched, the date-risk and staffing-risk signals stay separate. `lib/availability.ts`'s new `ownerLeaveCaveat(ownerId, commitments, from, to)` — pure, returns the earliest approved (non-Requested) Leave overlapping the window, or `null` — reuses the existing `commitmentCounts` predicate. Attachment point corrected during the build: not `DetailPanel.tsx`'s Schedule tab (the design's original citation, wrong — that tab has no owner field), but `OverviewTab.tsx`'s due-date row, which already shows `issue.owner` immediately above it. Null-safe against an owner name that doesn't resolve via `directoryIdByName` (silently no caveat, same stale-reference posture as `requiredSkills`). Scenario `HOL4` proves the overlap match, the out-of-window exclusion, the no-owner case, and — the one edge case worth naming — that a chronologically-earlier but still-*Requested* leave is correctly excluded in favor of a later *Approved* one. Clean `tsc`, 257 scenarios (+1, zero regressions), clean build, clean tenancy/attribution/restore audits. **Live-verify checked, 8 Sep**: both OAPIL's and SLG's Capacity tabs show "Nothing recorded in this window" — zero leave commitments exist in production today, so the caveat has no real data to render against yet. Confirmed, not assumed; a real render check waits on either a real leave being recorded or a deliberate test-and-clean-up pass |
| ~~I15~~ | ~~Leave report, month on month~~ | **Built, 10 Sep.** Design: `docs/plans/2026-09-08-leave-report-design.md` (open questions resolved by Nishant 10 Sep — trailing 6 months, reason text never shown full stop, firm-wide no per-project cut). Plan: `docs/plans/2026-09-10-leave-report-plan.md`. `lib/reports/leave.ts`'s `buildLeaveReport` is pure and computed live like every other report — only `Approved`/status-absent leave counts toward the person×month grid (`commitmentCounts`), clipped per month via the existing `workingDaysBetween`/`holidaySetOf` machinery; `Requested` leave surfaces separately in `pending`; `Returned` leave appears nowhere. Reason/note are structurally absent from the row types, not just omitted at render time. `LeaveReportView.tsx`/`LeaveReportDialog.tsx` are near-exact structural copies of the finance report's dialog/print-view pair, with no period picker (fixed window, per the resolved design) — wired into the Export menu right after "Finance timesheet…". Scenario `LV1` proves month-boundary clipping, holiday-awareness, the Requested/Returned split, and — a runtime assertion — that a planted reason/note never reaches `JSON.stringify(report)`. 262 scenarios (+1, zero regressions), clean `tsc`/build/eslint/tenancy audit across both commits. **Live-verified in production, 10 Sep**: Export → "Leave report…" renders the dialog and the print view correctly — honest "No approved leave in this window" (no approved leave recorded in OAPIL today), correct trailing-6-month period in the header, footer disclaimer intact. The `.xlsx` download button is correctly disabled when the report is empty, so the download path itself waits on real (or deliberately seeded) leave data to exercise, same posture I14's live-verify note already established |
| ~~I16~~ | ~~See an email's actual content, not just the truncated preview~~ | **Built, 8 Sep.** See `docs/plans/2026-09-08-email-body-view-design.md`. New `GET /api/mail/message/[id]` fetch-on-open route, same RAM-only/never-stored posture as the rest of `in-mail`, no new Entra consent. HTML bodies render inside a sandboxed `<iframe sandbox="allow-same-origin" srcDoc>` — no `allow-scripts` — rather than `dangerouslySetInnerHTML`, since untrusted sender content is a genuine XSS surface otherwise; the browser itself refuses any script, not a sanitizer this codebase would have to get right. Clean `tsc`, clean build (new route confirmed in the table), 257 scenarios unchanged (pure Graph passthrough, no reducer logic — same posture I8 established), clean tenancy audit. **Live-verified in production, 8 Sep**: opened a real message, full HTML body rendered correctly in the sandboxed iframe (mention link, signature, inline logo all intact). Inline images (`cid:`) and attachment download stay out, named as follow-ons. **Unblocks I18** |
| I17 | **Reply and schedule** | **Decided and built, 11 Sep — Option C, deployed as `ac84ff5`.** Nishant chose "draft into Outlook": the reply is composed in Axiomate and left in the person's own Outlook Drafts, and Outlook's own *Schedule send* owns the timing — no new storage, the RAM-only token posture untouched. `draftReply()` in `lib/personalGraph.ts` is the first two of `replyToMessage`'s three Graph calls (create the reply draft, PATCH the signed content in front of the quoted thread) returning the draft's `webLink`; `replyToMessage` is now textually `draftReply` + send, so plain Send is the same three calls it was; `POST /api/mail/reply` takes `mode: 'send' | 'draft'` defaulting to send; the reply dialog gains a secondary "Save to Outlook Drafts" beside Send (Send stays the one primary) and a drafted row badges "drafted · open in Outlook". Plan: `2026-09-11-reply-to-outlook-drafts-plan.md`. **Live verification is partial, and the reason is the feature's own posture**: the deploy restarted the app, which empties the RAM-only personal Graph cache, so "Your inbox" showed Reconnect and no message could be opened to draft against — reconnecting is an interactive sign-in only Nishant can do. Verified without a token: the route accepts `mode: 'draft'` and refuses with the same `401 reconnect` shape as send, and a missing message/comment is a 422 in both modes. **The step still owed**: after reconnecting, Reply on a real message → *Save to Outlook Drafts* → the draft appears in Drafts (the app's own folder picker can show it) with the reply above the quoted original and the signature beneath — then discard the test draft in Outlook. **Tried 11 Sep once I31 made the inbox reachable, and Graph refused the draft — a real finding, not a glitch**: `createReply`/`createReplyAll` require the delegated `Mail.ReadWrite` permission (Microsoft's reference lists it as least-privileged with no alternative; `Mail.Send` covers only the single-call `reply` action and `send`), and the app requested `Mail.Read Mail.Send`. So the three-call signed-reply sequence I19 shipped on 10 Sep has been refused by Graph since that day — I19's live check covered the template and the signature body, not a personal send — and Option C inherits the same gap. **Fixed in code**: `AXIOMATE_DELEGATED_SCOPES` now carries `Mail.ReadWrite` in place of `Mail.Read`. **What only Nishant can do**: delegated `Mail.ReadWrite` needs no admin consent, but each person sees one consent screen on their next sign-in — the refresh token minted under the old scope string is refused (`invalid_grant`), the token store deletes it, and "sign in once" is the hop that grants it. The draft test runs after that consent. **Verified live, 11 Sep, after Nishant consented (per-user consent chosen over tenant-wide admin consent)**: Reply on a real inbox message → *Save to Outlook Drafts* → the row badged "drafted · open in Outlook" with a live link; the app's own folder picker switched to Drafts listed "RE: Microsoft 365 security: You have messages in quarantine", and its opened body read, in order, the reply text, the signature block (name, title, firm, tagline, phone) and the quoted original — exactly the sequence `draftReply` prepends. Every check read from the DOM. One test draft is left in Nishant's Drafts to discard by hand — the app has no delete and the M365 connector's token had expired. **I17 is complete.** Originally: scoped 8 Sep, not built. Design: `docs/plans/2026-09-08-scheduled-reply-design.md`. **Not a small extension** — flags a real architectural fork before any code: the Graph token cache is deliberately RAM-only and never persisted (`lib/db/personalGraphTokens.ts`'s own stated posture), which a genuinely-timed scheduled send would need to survive an app restart. Three options laid out (A: durable encrypted token store — a posture reversal affecting every personal-Graph feature at once; B: session-scoped best-effort scheduling that fails visibly rather than silently; C: draft into Outlook and let Outlook own the actual send timing — recommended as the only zero-new-risk option, but not decided here). Needs Nishant's call on which, or whether "schedule" means something narrower |
| ~~I18~~ | ~~Create an issue from a specific email~~ | **Reconciled and closed, 9 Sep.** Design: `docs/plans/2026-09-08-create-issue-from-email-design.md`, updated with what it missed. The original draft (scoped 8 Sep, written before checking) proposed a second "Create issue…" button reusing `classify`/`draftFor`/`provenanceNote` — but mail-filing already existed (`2026-08-31-in-mail-design.md`'s "File as work item", `POST /api/mail/file` → `lib/mailFile.ts`'s `mapGraphMessage`), built over a week earlier. The real gap: `mapGraphMessage` hardcoded `type: 'Request'`/`severity: 'Medium'` on every filed mail instead of classifying — an invented default (operating principle 2). **Built**: `mapGraphMessage` now calls `draftFor` directly (not `classify`, which would refuse `no-mailbox` on nearly every personal-inbox message) for real type/severity. Clean `tsc`, clean build, scenario `IM1` extended to the real call signature and still PASS (259 scenarios, no regressions), clean `audit:tenancy`/`audit:attribution`. **Decided**: whether filing should route through the "Add Work" dialog for review before creating (this draft's other half) — asked Nishant, answer was **keep instant create**. No further work |
| ~~I19~~ | ~~A signature on outgoing mail, one format for the whole firm~~ | **Built, 10 Sep.** Design: `docs/plans/2026-09-08-email-signature-design.md` (all questions resolved by Nishant 10 Sep — new `Person.title` and `Person.phone` fields; scope widened to every outbound path including the firm-mailbox route, using the signed-in sender's own identity; the template is a stored, editable `OrganizationIdentity.signatureTemplate`, seeded with the decided content including the locked positioning tagline). Plan: `docs/plans/2026-09-10-email-signature-plan.md`. `lib/signature.ts` is pure: fills `{{name}}`/`{{title}}`/`{{org}}`/`{{phone}}`, drops a line whose value is absent (never a blank line or literal placeholder), HTML-escapes every value, composes the logo as an `<img>`. `replyToMessage` rewritten from the single-call `/reply` to createReply → PATCH → send, because Graph's own docs make `comment` and `message.body` mutually exclusive and leave `comment`'s HTML handling undocumented. `sendAsMailbox` gains a trailing optional `contentType` defaulting to `'Text'` — its four other callers (Daily IMS, governance pack, notifyDrain, workspace notify) are byte-identical, confirmed by grep. The firm-mailbox route's client-visible note and retry-dedupe keep reading the plain text; only the Graph send gets the signed HTML. `SIG1`/`SIG2` scenarios; 245 scenarios (+2, zero regressions) across three commits, clean tsc/build/eslint/tenancy throughout. **Live-verified in production, 10 Sep — two real findings, both fixed**: (1) the signature template was **empty** on the live workspace — `DEFAULT_ORGANIZATION` seeds only brand-new workspaces, and this one predates the field, so every signature would have been blank; filled in through the Configuration UI itself (the tool built for exactly this), confirmed persisted after reload. (2) Rendering the live profile's actual output showed the typed message running straight into `Thanks,` with no gap — `buildSignatureHtml` now opens with a blank line, `SIG1`'s exact-string assertions updated. Nishant's own title/phone set. Title/Phone columns and the template textarea confirmed working via DOM reads (this session's browser screenshots were unreliable — DOM state was checked directly instead). **The actual send is Nishant's to do**: it needs his personal-inbox OAuth connection ("sign in once" on the Inbox panel), an authentication step not performed on his behalf; the firm-mailbox route sends real client mail and was not test-fired. The expected rendered result for his profile: a blank line, then `Thanks,` / `Nishant Sekhar` / `Chief executive officer` / `Axiocloud Solutions` / `Engineering Intelligent Enterprises` / `+91-9035001871`, plus the logo once one is uploaded (none is today) |
| I20 | **UX audit — four confirmed quick wins, built and verified live; two original findings disconfirmed** | Built, 8 Sep. Full audit published as an artifact (linked in session); `docs/pending-actions.md` note only, no design doc — findings were direct code/UI fixes, not features needing scoping. **The re-verification story matters here**: the first audit pass ran partly against a browser tab with corrupted zoom/viewport state (traced to an earlier failed screenshot action), which produced two false findings (Kanban's empty-column collapse and Tree's truncation tooltip both already work correctly — confirmed via DOM inspection in a clean tab) — both are struck through in the republished artifact rather than silently dropped. **Fixed and verified**: (1) DetailPanel's tab bar overflow — 11 tabs on an issue row clipped "Links"/"History" off-screen with no scroll affordance (`scrollWidth` 692px vs `clientWidth` 533px, confirmed); added a `.tabs-wrap` fade gradient + visible thin scrollbar, verified visually harmless on a non-overflowing 4-tab project row and a correct hint on an overflowing 11-tab issue row. (2) Raw `WT_*` codes leaking into the Work Type filter and DetailPanel's Type field — root cause traced precisely: every work type already has a real `label` in config (`liveWorkTypes`), and `FilterDropdown` already supports value/label pairs (used by Discipline) — Work Type's facet just wasn't using either. Fixed by mirroring Discipline's own pattern; filtering still compares stored values, only the display resolves through the label. (3) A one-time migration toast ("Work saved in this browser...") that re-fired on every page load forever, contradicting its own code comment's stated intent ("said out loud... once") — gated behind a per-tenant `localStorage` dismissed flag. (4) **A new bug found only during re-verification, unrelated to the fork's original list**: the mobile-nav hamburger button was visible and dead-clicking at every desktop width — a CSS specificity collision (`.nav-burger{display:none}` and `.btn{display:inline-flex}` are both one-class selectors, and `.btn` being later in `globals.css` silently won the tie regardless of the `@media(max-width:900px)` block). Fixed by raising the selector to `button.nav-burger`; confirmed live that the button now correctly hides at 1440px with the sidebar unaffected. **Reclassified, not independently fixed**: the "inconsistent person-name display" finding (Owner header shows "Nishant", Raised By shows "Nishant Sekhar") turned out to be the literal free-text value of the Owner field, not a formatting bug — folds into the still-open Owner-directory-validation item from the audit (not yet built; needs the legacy-value migration-policy decision named in the audit before it can be built). **Re-verified since, confirmed real and now built**: required/optional field marking in Add Work (Subject, Owner, Next Action, Description carried no marking; Planned start/Due date did) — every field in `Dialogs.tsx`'s `AddForm` that the `create` reducer arm doesn't actually require (confirmed by reading the validation directly: only `name` is checked) now carries an `hint="Optional"` label, matching the existing convention. While checking it, found the deeper work-type data issue tracked as I21 below. **Re-verified and now built**: Analytics' tables-only rendering — added a pure-CSS "data bar" (`linear-gradient` background on the count `<td>`, no library) to all five numeric columns across its four tables, each scaled to its own column's max so a reader spots the outlier at a glance; empty/all-zero columns render no fill rather than dividing by zero. **Live-verified in production**: OAPIL's 91/46 fully fills its column against SLG's 31/11, the 0-7-day age bucket (89) dominates visibly against 90+ (0, no fill) — reads correctly at a glance exactly as intended. **Re-verified, confirmed real, and now built**: Calendar's grid/rail imbalance (measured directly: the grid took 914px/78% of the content width for 9 scheduled items this month, the unscheduled rail took 260px/22% for 82 items). Fixed as an automatic threshold, not a toggle, per Nishant's choice — when unscheduled items exceed 50% of the filtered set (`m.undated.length / rows.length`), the rail (`.cal-rail.dominant`, `flex: 1.3 1 320px`) leads over the grid (`.cal-grid-wrap.secondary`, `flex: 1 1 320px`) instead of sitting in a fixed 260px column. **A second self-correction, caught before it shipped**: the original "rail items truncate with no title attribute" half of this finding was itself an artifact — my own diagnostic script sliced the text to 50 characters for its own display and I mistook that for evidence of browser clipping. Direct measurement (`overflow: visible`, `whiteSpace: normal`, `scrollHeight === clientHeight`, full text present) showed nothing was ever cut off; the rail wraps the full name normally. Removed that half of the claim rather than let it stand. Added `title={row.name}` to the rail row anyway as a harmless, minor nicety, not as a bug fix. **Live-verified in production**: `.cal-rail.dominant` measures 622px (53%) against `.cal-grid-wrap.secondary`'s 552px (47%), exactly matching the pre-deploy preview. Clean `tsc`, clean build, 257 scenarios unchanged (all UI/CSS, no reducer logic touched), clean tenancy audit |
| ~~I21~~ | ~~134 issues carry a raw work-type code (`WT_EPIC`) instead of its label (`Epic`), with a matching 9-entry duplicate registry~~ | **Built, 9 Sep.** See `docs/plans/2026-09-08-work-type-data-cleanup-design.md`. User's direct instruction ("Put a clean data foundation") resolved the open question toward Option A — migrate, not leave-and-dedupe. `scripts/migrate-worktype-labels.ts` (dry-run default, `--apply` to write, data-driven — no hardcoded pair list) corrected 134 issues through the normal audited `updateIssue` path: Task 64, Epic 21, Issue 15, Decision 14, Request 8, Deliverable 5, Risk 3, Defect 2, Change Request 2. **A dependency check before running found a genuine bonus fix**: `lib/raid.ts`'s `raidKindOf` resolves `type` to a `WorkType` by label then checks that entry's id — the 17 raw-coded Risk/Decision issues were resolving to the *duplicate* entry (wrong id) and were silently not being recognized as RAID records at all; the migration fixes this as a side effect. All 9 duplicate `WorkType` entries then showed 0 records and were archived through the normal Configuration UI, unlocked by its own `disabled={used > 0}` guard. Live-verified: `WT_EPIC` correctly shows 21 records, `WT_RISK` 3, `WT_TASK` 64; "All settings" now reads "11 types" (down from 20). **Note on execution**: writing and running the migration script required explicit in-conversation approval at each step (file write, then the dry run, then `--apply`) — the permission classifier correctly refused to let the assistant self-grant a settings.local.json permission rule when asked, which is the right boundary |
| ~~I22~~ | ~~A people directory (readable by any colleague), join/exit dates, and an onboarding flow~~ | **Built, 8 Sep.** See `docs/plans/2026-09-08-people-directory-onboarding-design.md`. New `PeopleDirectory.tsx` screen (gated on `internal.view`, not `config.manage`) opens the existing `ProfilePanel` unchanged — the content already existed, the gap was discovery. Two new optional `Person` fields, `joinedOn` and `status: 'Active' \| 'Departed'` + `departedOn`, following the codebase's own absent-versus-cleared merge convention exactly, including one new edge case (`status: 'Active'` clears both `status` and `departedOn` together; an untouched later edit carries the Departed state forward) — proven by new scenario `JD1`. Departed people drop out of the *manager* picker only in this build (both the People config card's row select and onboarding's); Owner, Allocate-to-project and Next-action assignment are not wired to `status` yet — named explicitly rather than overstated. **A permission gap found and closed during review, not asked for**: `loadStoredView()` restores any `WorkspaceView` from `localStorage` with zero permission check — true for `analytics`/`mail` too (pre-existing, not fixed here), but `people` is the first of the three to expose personnel data directly, so `IssueWorkspace` now redirects away from it on mount and on render whenever the actor isn't internal. **A CSS bug caught in the same pass**: the departed-row style originally reused `.prov` (muted color), which also sets `font-size: 11px` and `font-style: italic` — cascading into the whole row including the clickable name button via the `button { font: inherit }` reset. Replaced with a dedicated `.pdir-row-departed` (color only). Three open questions resolved with low-stakes defaults per "continue with backlog": join/exit fields stay `config.manage`-gated, no future-dated departure in v1, People placed in the existing Records nav group. Clean `tsc`, clean build, 258 scenarios (+1, zero regressions), clean `audit:tenancy`/`audit:attribution`/`audit:restore` |
| ~~I23~~ | ~~A daily report on progress of engagement~~ | **Built, 9 Sep.** See `docs/plans/2026-09-09-daily-ims-engagement-scope-design.md`. Almost entirely already existed: `lib/reports/dailyIms.ts`'s Daily IMS (position, 24h movement from the audit trail, six actionable sections), and the 30 Aug report-delivery design already auto-emails it every weekday as a branded PDF. Checked live: **the toggle was already on** (`imsEnabled: true`, recipient `sekharn@axiocloudsolutions.com`), and the scheduled pass confirmed running (last ran 2026-09-09 01:30 UTC via the Logic App live since 17 Aug). **The one real gap**: the auto-send always covered `'All clients'`, hardcoded, no way to narrow it — manual on-screen export already supported per-engagement scope, the scheduled one didn't. Added `ReportDeliveryConfig.imsScopeNodeId` (null = unscoped, default, byte-identical to today; set = narrows to that node's subtree via `underScopeOf`, exported from `lib/reports/clientPack.ts` rather than a third copy), validated against `state.nodes` in `setReportDelivery` (a deleted or invented scope is refused, not silently mailed as an empty report — proven by scenario `DL2`), and a "Scope the IMS to" selector in Configuration → Scheduled pass → Report delivery listing live engagement-tier nodes. **Extended same day, per Nishant's follow-up ("Automate for every engagement with proper indentation")**: a genuinely ambiguous instruction, checked directly rather than guessed since it touches a feature that emails people automatically — two concrete shapes were put to him (one combined report with indented sections, vs. one separate email per engagement) and he picked the latter. Added `ReportDeliveryConfig.imsPerEngagement` (default `false`, overrides `imsScopeNodeId` when on) and a "Send one automatically for every engagement" checkbox — `runDelivery` now loops every live engagement the same shape `sendPacks` already uses for clients: one PDF per engagement per recipient, an empty engagement skipped, the batch's stamp only advancing once every non-empty send succeeded. Clean `tsc`, clean build, 259 scenarios (DL2 extended, not a new one, zero regressions), clean `audit:tenancy`/`audit:attribution`/`audit:restore`/`audit:persistence`. **Note on verification limits, named rather than glossed over**: the send-time fan-out logic itself (which engagements get skipped, whether the stamp advances) is not reachable by the pure scenario harness — checked by direct code reading against the packs' already-proven pattern, not by running it against production, since running it would mean actually emailing people |
| I24 | **The Work/Sub-Work detail page needs better UI/UX** | Checked live, 9 Sep — no design doc, one direct code fix plus a design observation, matching I20's convention. "Work" and "Sub-Work" turned out to be tree-tier labels (a node's place in the WBS), not `WorkType`s — both are `kind: 'issue'` and get the identical `DetailPanel.tsx`, confirmed by opening one of each (SLG-001/SLG-002) side by side; no divergent bug pattern between them. Checked Overview, Schedule, Links, Checklist tabs — Schedule, Links and Checklist are solid, no findings. **Fixed and built**: the sticky header strip's Owner field is a plain `<input>` (`field-strip`/`fs-fld`, `components/DetailPanel.tsx`) bound to the same free-text owner value the audit's still-open directory-validation item already names — native inputs never apply `text-overflow: ellipsis` to their own value text, so a dual owner like "Michael Thomas (POS) / Amolak (D365)" hard-clips to "Michael Thomas (POS) / Amol" with zero indication anything is hidden (confirmed via DOM: `scrollWidth` 205px vs `clientWidth` 135px, no `title` attribute). Added `title={ownerDraft}` — the one thing a clipped input can offer for free, a hover tooltip; the same minimal, low-risk pattern as the Calendar rail-row nicety from I20. Clean `tsc`, clean build; a display-attribute change with no reducer logic, so no new scenario, matching how I20's own CSS-only fixes were noted. **Flagged, not built — a design call, not a bug**: the compact header strip and the read-only summary table below it both show Status/Owner/Due Date/Severity, just in two different forms (quick-edit vs. full reference) — real vertical-space cost, but removing either is a genuine design decision the audit didn't ask this pass to make |
| I25 | **Issue detail panel redesign — "reads like a database form"** | **Tier 1 and all of Tier 2 (items 7–10) built and confirmed working live, 9 Sep. I25 is complete.** Design: `docs/plans/2026-09-09-issue-workspace-redesign-design.md`. Nishant pasted a UX critique of the detail panel (written against generic mockup screenshots, not this codebase) and asked for a full design doc before any code. Reconciled against what's actually built: the critique's central layout recommendation (a three-pane width ratio) doesn't apply — the panel is bottom-docked and resizes by height, not a right-side pane sharing width with the tree. A pinned KPI-style header (`FieldStrip`) already existed, addressing the same diagnosis the critique arrives at independently. **Tier 1, built same day on "Build the Tier 1 items"**: a real title (`.detail-title`, `row.name`, survives `compact` state); Severity restyled as a colored tag reusing the existing `sev-High/Medium/Low` palette (Status was NOT — a real palette check found none exists for status, unlike severity, so that stayed in Tier 2); I24's flagged Status/Owner/Due/Severity duplication resolved — Subject/Severity/Status/Owner rows removed from Overview's `<dl>`, checked precisely so Start-Due (carries Start Date too) and Raised by/Accountable (not in the strip at all) correctly stayed; description capped at 3 lines with a ref-measured "Show more" that never appears when nothing's hidden; Reply/Schedule/Teams collapsed from three stacked sections to one button row, each still opening its full untouched form; the two `<dl>` columns split into four named groups (Record/Classification, Ownership & timeline/Progress) without changing the two-column grid. Clean `tsc`, clean build, 259 scenarios unchanged (display-only, no reducer touched), clean `audit:tenancy`; two pre-existing `audit:a11y` failures confirmed unrelated (different files, untouched by this change). **Live-verified in production against a real issue (SLG-001) and found two real bugs the build-time checks couldn't catch, both fixed same day**: (1) the description clamp didn't clamp — `-webkit-line-clamp` doesn't count wrapped text lines through a nested block child (`RichTextEditor`'s `.rte` > `<p>`), confirmed by `scrollHeight === clientHeight` on visibly-long text; switched to a plain pixel `max-height`, which works regardless of nesting. (2) the severity tag rendered every severity in the same muted grey — `.fs-sev` is a direct `span` child of `.fs-fld`, so the existing `.field-strip .fs-fld > span` label-styling rule (higher specificity) was silently outranking `.sev-High`/`.sev-Medium`/`.sev-Low`; fixed with three selectors specific enough to win. Both found by computed-style inspection, not by eyeballing a screenshot. **A third bug found re-verifying the fix rather than assuming it was complete**: once the clamp genuinely clipped the description, "Show more" never appeared — the one-shot measurement effect raced `RichTextEditor`'s (TipTap, `immediatelyRender: false`) async content mount, read a near-empty box before content painted, and nothing in its dependency list ever changed again to trigger a re-measure. **First fix (a `ResizeObserver` on the clamp's children) also failed re-verification** — redeployed, re-checked live, still stuck `false`: a `ResizeObserver` needs an existing node to watch, and TipTap's content node doesn't exist yet at attach time. Confirmed the mechanism directly against the live page (a console-level `MutationObserver` fired on a real content change; an equivalent `ResizeObserver` didn't) before writing the real fix: a `MutationObserver` on the clamp's whole subtree, which fires on the insertion itself with no existing node required. Clean `tsc`, clean build, 259 scenarios unchanged. **Confirmed working on a true fresh page load** (the scenario that matters — a real user's first click, not a re-selection of an already-mounted issue): clamp genuinely clipped (196 vs 60), "Show more" present, clicking it correctly expanded and flipped to "Show less." All six Tier 1 items confirmed live together on SLG-001. **Tier 2 scoped 9 Sep, not built.** Re-scoped from four vague bullets to concrete specs by reading further into existing code: (7) status color palette — fully specified, composes `TERMINAL_STATUSES`/`BLOCKED_STATUSES` (`lib/schedule.ts`) with the existing `--h-*` schedule-health tokens, no new modeling; `--h-complete` sits close to `--sev-low`, so **decided 9 Sep, revised once**: a first pass proposed a solid `background: var(--h-*)` fill, but checking the tokens found `--h-*` is a *foreground*-only color in both themes (light mode's own AA-contrast comment confirms it, `globals.css:37`), so a solid fill would need the token to flip roles per theme — the same isolated-rule-vs-cascade pattern that caused the Tier 1 severity bug. Settled on a heavier `currentColor` tint instead (`.fs-status`: 28% mix, no border, vs. `.fs-sev`'s existing 10% mix with a border) — one rule, works in both themes automatically, same mechanism `.fs-sev` already proved live. (8) tab consolidation — **mechanism now recommended, 9 Sep**: adaptive priority-navigation (render in priority order, collapse only what doesn't fit the available width into `⋯ More`), not the originally-proposed hardcoded 4/7 split — sourced from Apple HIG's 5-tab-max convention and Nielsen Norman's ~5-item scannability guidance, with real precedent in GitHub's repo-nav overflow and Salesforce Lightning's tabset component; current behavior is `.tabs`' horizontal scroll-with-fade (`globals.css:1569-1613`), which keeps every tab one click away but doesn't solve the "reads like a database form" diagnosis on its own. Adaptive over hardcoded specifically because no per-tab usage data exists anywhere in this codebase to check a permanent guess against — this way the only real decision left is the priority order, not an irreversible cutoff. **Priority order itself still stays Nishant's call**: `Overview, Checklist, Discussion, Time` proposed as the default first four, confirm or reorder before building. (9) Time KPI strip — placement recommended inside `FieldStrip` using data `effortVariance()` already computes, zero new data needed. (10) context-sensitive primary action — `DEFAULT_STATUS_POLICY.transitions`' existing ordering already encodes "the ordinary next move" (`allowedNext(policy, status)[1]`), so labels come nearly free; checked the refusal path and found the "Confirm closed" row (requires evidence) would otherwise dead-end on a plain refusal string, since `checkTransition`'s `'evidence'` kind isn't routed through the same inline prompt `'reason'` gets (`lib/board.ts:53-67`, `DetailPanel.tsx:1335-1348`). **Decided 9 Sep, placement corrected once**: a first pass put the button in `OverviewTab` because `onManageEvidence` was already a prop there — wrong, because `chooseStatus` and the reason-prompt UI the other four rows need are private to `FieldStrip` and not reachable from `OverviewTab`. The button lives inside `FieldStrip` instead, calling `chooseStatus` directly (reusing the reason-prompt path with zero new code); the real new work is threading `onManageEvidence` into `FieldStrip` as a new prop and having `chooseStatus` call `checkTransition` directly (not just `dropOutcome`) so the `'evidence'` kind can be told apart and routed to it. **Items 7, 9, 10 built and live-verified same day (9 Sep)**: clean tsc/build/259-scenarios/audit:a11y; two specificity/inheritance issues caught and fixed before shipping (`.fs-time-val` would have hit the same generic-label-wins-over-specific-class trap as Tier 1's severity bug, and would have rendered "18h / 32h" as "18H / 32H" via inherited uppercase) — both fixed before the first live check, not discovered by one. Live-verified against SLG-001 post-deploy: status tag renders the correct `--h-ontrack` color with no border (severity re-checked alongside, unaffected); the Time KPI correctly hides when nothing's estimated (confirmed on SLG-001, which has none) and the has-estimate rendering verified via a synthetic DOM probe against the real cascade; item 10 exercised end to end — moved SLG-001 to "Awaiting client confirmation", confirmed the suggested label updated to "Confirm closed", clicked it with zero evidence attached, and got the Evidence & Documents panel opening directly with no dead-end refusal message, exactly as decided. Test data reverted afterward, live record left clean. **Item 8 (tab consolidation) built and live-verified same day**: `TabsBar` replaces the flat 11-tab row with priority navigation (cached widths, `ResizeObserver`-driven, active tab always forced visible, overflow under `⋯ More`). Two CSS cascade bugs found live, neither a logic bug — the underlying width-fitting was correct from the first deploy: (1) the wrapper around `.tabs`/`⋯ More` had no `display:flex`, so More wrapped onto its own line — fixed; (2) `.tabs-more-menu` computed `position: absolute` instead of the `fixed` it was written to have, `.menu`'s later-declared rule winning on source order at equal specificity — fixed by stacking both classes (`.menu.tabs-more-menu`), the menu was already fully functional at the wrong position value (confirmed via `elementFromPoint` hit-testing and a real click-through) so this was latent, not broken. Both resize directions (shrink → collapse, widen → recover) confirmed live without a full re-render needed to re-measure. **I25 is complete — Tier 1 and all four Tier 2 items built and confirmed working live** |
| ~~I26~~ | ~~Open-ended ask ("look for more improvement"): a fresh live audit plus a security check~~ | **Two things found and fixed, 10 Sep.** (1) **Security**: `npm audit` found a critical unauthenticated RCE in Next.js's Image Optimization API, plus two supply-chain highs (`fast-uri`, `sharp`) — all three patched via `npm audit fix` (16.2.9's existing semver range already covered the fix, no `package.json` change needed). Left alone: `mysql2`/`deepmerge-ts`, both only fixable via a `prisma` 7→6 downgrade that would undo the F2/F4 driver-adapter migration — both are transitive deps of the Prisma CLI itself, not reachable in the deployed `.next/standalone` runtime, so dev-tooling exposure, not production. (2) **A live UX audit found a real bug**: Board's per-card "Move ▾" status menu could not be closed — not by an outside click, not by Escape, not by re-clicking its own toggle. Root cause confirmed directly (`elementFromPoint` on a click inside the scrim's own bounding rect returned `<body>`, with `#app-shell` marked `inert=""`): `useOverlay`'s background-disabling `inert` mechanism assumes the overlay itself is portaled outside `#app-shell` (as `RowMenu.tsx` correctly does); `MoveMenu` claimed to follow that shape in its own comment but actually rendered inline inside the card, inside `#app-shell` — so opening it made its own scrim, menu and trigger button inert along with the background. Fixed by actually portaling it (`createPortal` to `document.body`, viewport-coordinate positioning computed by the caller the same way `TreeGrid`'s `openMenu` already does for `RowMenu`) rather than the CSS-relative positioning that only worked while un-portaled. Live-verified all three dismissal paths after the fix; clean `tsc`/build/eslint, 261 scenarios unchanged (display/event-wiring only, no reducer touched) |
| ~~I27~~ | ~~Three more `default*()` functions likely share the "won't reach an already-provisioned tenant" gap I1/I10 already found and fixed workarounds for~~ | **Checked live, 11 Sep — one of the three had drifted, and it is fixed in production.** The live model was read from the running page's own React state (the `operating-model.v1` browser mirror turned out to be stale evidence — it lacked I19's `signatureTemplate`, which is verified live — so it was discarded) and compared with today's `default*()` output computed locally. `approvalRules`: the default `APPR_CR_START` present, plus one tenant-added rule (`imported-historical`) — data, not drift. `automationRules`: all five defaults present and enabled, including `AUTO_SUBWORK_CLOSED`. `statusPolicy`: byte-identical. `healthScore` (I30's new field): present, which also proves the `case 'config'` whole-model upsert carries new keys. **`watch.conditions`: six stored against seven shipped — `allSubworkClosed` had never reached this tenant.** Mechanism confirmed in `mergeModel` (`lib/config.ts`): a stored `conditions` list replaces the seed's wholesale, deliberately ("a firm that removed one means it"), so a condition added to the defaults after provisioning is exactly the I11 shape. **Consequence in production until today**: the scheduled pass never raised `issue.allSubworkClosed`, so the enabled `AUTO_SUBWORK_CLOSED` rule ("tell the owner when every piece of sub-work under it is closed") had never fired. **Fixed by the product's own control**: Configuration → Scheduled pass lists every `WATCH_CONDITIONS` entry with a checkbox, and the missing one was visible-but-unticked — ticked it ("Scheduled pass updated."), reloaded, and the live model reads seven. By the section's own note, its findings are recorded on the next run and raised from the one after, so nothing historical floods out. No code change: the merge rule is a stated decision, and the UI already exposes the gap; what this row asked for was the check. Originally: surfaced by the same audit as I26, structural scan half. `defaultApprovalRules()`, `defaultWatchPolicy()`, and `defaultStatusPolicy()` each seed a per-tenant stored snapshot the identical way `defaultAutomationRules()` does — and only `automationRules` has an explicit UI workaround for a newly-shipped default never reaching an existing tenant (I11's "Add rule" button). Not verified live — doing so would mean simulating a stale already-provisioned tenant, which this session's production tenant already isn't (it's been kept current by hand every time this exact gap was hit). Worth a five-minute check next time Approvals, the watch policy, or the status policy's own defaults are touched: does the already-provisioned production tenant's stored `approvalRules`/`watchPolicy`/`statusPolicy` actually reflect what `default*()` would produce today, or has it drifted the same way `automationRules` would have without I11's fix |
| I28 | **A second audit pass — My work/calendar/week, Mail, and most of Configuration — found nothing worth building, and one thing worth correcting rather than silently dropping** | Continuation of I26's audit, into the screens it hadn't reached. **My work, My calendar, My week, Mail/Inbox, and every Configuration tab checked before the false alarm below (Terminology, Roles & people, Status transitions, Workflows & templates, Permissions, Approvals) — all confirmed clean, live.** No second instance anywhere of the Board bug's exact shape (a popover made inert by its own un-portaled overlay) — checked specifically, found none. **A reported "Configuration's whole content panel never renders" finding was a false alarm from the audit's own browser session, not a real product bug — checked directly and corrected rather than logged as-is.** The claim was specific to a 740×406 CSS-pixel viewport; re-tested directly at a genuinely normal laptop size (1440×791, where `resize_window` itself had no effect either way) and both the "All settings" card grid and an individual panel (Terminology) rendered correctly, fully populated. The underlying CSS was also checked directly rather than taking the claim on faith: `.cfg-rail` already carries `overflow-y: auto` — the exact independent-scroll pattern that would be needed if a real narrow viewport ever did starve it for height — so the claimed root cause (no scroll allowance) does not match what is actually in `app/globals.css`. 740×406 is not a size any real browser window organically renders at; this session has independently hit unexplained narrow/stuck-viewport artifacts in this exact browser-automation environment before (noted earlier, I25's session). Two minor, low-priority items surfaced and left as-is rather than built: the "Add meeting" modal (`MyCalendarPanel.tsx`) doesn't close on Escape — but its own code comment states this is deliberate ("keyboard dismissal via the Cancel button"), not an oversight, so left alone rather than overridden on a guess about why; and Configuration's "All settings" card grid omits ten real, reachable categories (Permissions, Approvals, Automation, Scheduled pass, Time recording, Allocation, T-shirt sizing, Responsibilities, Activity templates, Issue templates) from its own index — a minor navigational inconsistency, not investigated further this pass since it may be a deliberate curation rather than an omission |
| I29 | **Mail triage: unconfirmed issues and a private personal action list** | **Built, 10 Sep — commits 0–3 landed** (schema + migration with RLS, the `PersonalAction` entity and its four reducer arms, the `needsTriage` clearing rule, the intake pipeline's one new dispatch, and the UI: a "Needs triage" chip beside "RAID only", Confirm / "Move to my to-dos" on an unconfirmed record's Overview, and a private **My to-dos** view beside My calendar; proven by `PA2`/`PA3`/`TRG1`/`TRG2`). The boot-time redaction (`personalActionsFor` in `redactForReader`, zeroed in `clientView`) went in with the UI commit, ahead of any screen — the plan had it as its own step and it was nearly missed. Two things found on the way and not fixed here: the `triageOnly` boolean initially counted as an active filter at rest (`CD5` caught it; `isActiveFilter` now names it beside `raidOnly`), and the local-only `audit:a11y` (not in CI) reported two **pre-existing** errors untouched since I22 — `AnalyticsView.tsx:61`, `PeopleDirectory.tsx:50`, both the same pointer-only scrim line — zero new; **fixed the same day at Nishant's "keep correcting, don't leave"**, with the exact disable-with-justification form `MyWorkPanel`/`PortfolioPanel`/`EvidencePanel` already carry for the identical line, after confirming both files wire `useOverlay` so the stated Escape keyboard path is true for them. The audit is clean now. **Live-verified in production, 10 Sep** (every commit's run concluded `success`, confirmed via `gh run view`, not a filtered stream): My to-dos is in the sidebar and renders; a to-do was added (Add correctly disabled while empty), marked Done (left the open list, "Show done (1)" appeared, struck through), and removed — production left clean; the "Needs triage" chip sits beside "RAID only", toggling it drops the grid to 0 / 136 (no unconfirmed records exist yet), the Filters chip counts it only while on (1 → 2), and toggling back restores 92 / 136. Screenshots were unreliable in this browser-automation environment, so every check read the DOM directly. **Still unverified, by construction**: Confirm and "Move to my to-dos" only render on a record intake filed from a genuinely new thread, and none exists in production yet — the next real inbound thread is that test. A step the plan's first draft lacked was found at implementation: `personalEvents` is five things (model, mappers, loader, writer, mirror seed), so a standalone schema commit was added and the plan corrected in that commit. `PA3`'s negative case was corrected too — a roleless directory person holds the fallback role, which ships as Administrator, so the negatives are the two client seats. UI (filter chip, triage actions, My to-dos) still to build. Originally: designed and planned 10 Sep. Design: `docs/plans/2026-09-10-mail-triage-and-personal-actions-design.md`; plan: `2026-09-10-mail-triage-and-personal-actions-plan.md` (9 steps, 3 commits). Started as a reported "can't see the mail body" bug that turned out to be about a different screen — Mail log has no triage, by the 24 Aug design's own deliberate choice, now reopened with that named. Grounded in Hive, Missive and Jira Service Management before the shape was settled; JSM's "auto-create immediately, triage via a queue" overturned this design's own first draft (holding creation back), which would have left a new thread with no record at all. Shape: the intake create path is byte-identical, one additive marker `Issue.needsTriage` (a plain boolean — the plan corrects the design's absent-means-unrecorded sentinel, which solves a problem this flag doesn't have), a `triageOnly` filter chip mirroring `raidOnly`, and a `PersonalAction` entity that is a near-exact copy of the already-shipped `PersonalEvent` (same absolute per-owner redaction in `redactForReader`, no exemption for ADMIN). "Your inbox" → File already covers the personal-mailbox half and is untouched. Highest-risk step named: the `updateIssue` clearing rule running for real once intake sets the flag, guarded by `TRG1`'s no-op-edit assertion |
| I30 | **The F&O page grammar — the shell reshaped to the patterns its users already know** | **Built, 10–11 Sep — steps 1–8 landed as six commits, every run concluded `success`** (score logic + `setHealthScore` + pack score + `PF3`/`PK4`; the drawer as a details page — `ID : Subject`, status chip, single-open FastTabs, one primary; the FactBox blade; list-page discipline — quick filter in the always-visible Filters row bound to the same `filters.search` as the global search, first-column links, `RECORD_ISSUE_PLURAL` in the terminology layer; My work and Portfolio as workspaces with tiles, Configuration → Health score in Governance, packs rendering the score with its disclosure; the design docs refreshed from nine views to thirteen and 27 sections to 30). Found on the way: `.btn-link` was used by ten components and styled nowhere — the first-column link existed in markup only; `scoreTerms()` and `CONCERN_LABEL` were added so the tile, the pack and the Configuration note print one sentence; 4b shrank to a one-primary enforcement because `SelectionToolbar` + `FieldStrip` already were the action pane; step 4 split into 4a/4b at implementation. **Step 9 live-verified in production, 11 Sep, every check read from the DOM** (screenshots stay unreliable in this browser environment): an issue opens as `OAPIL-120 : Scope …` with an `In Progress` chip upper-right, exactly one primary (`Send to client`, the suggested transition), eleven FastTabs with header summaries, Overview fitting without scroll; the Related information blade opens four FactBoxes, widens the drawer to 1020px, and its Owner/Schedule agree with the strip; **the plan's guard holds** — from a closed drawer, a Tree row's `Log time` opened the drawer with the Time FastTab open and in view; the quick filter narrows the same state as the global box (typing mirrored into it; `OAPIL-120` → "1 of 136 work"; a subject word → 6) and the caption reads the plural term ("92 of 136 work"); My work's tiles equal the Tree — Showing 92 = "92 of 136", the Overdue tile (8) landed on the Tree with `health=Overdue` reading "8 of 136 work"; Portfolio tiles print band, value and terms (OAPIL 6 amber = "Blocked 1×2 + No owner 4×1"); Configuration → Health score accepted "No owner" 1→2 with its toast and the tiles moved to 10 red / 6 amber with "No owner 4×2" printed, then restored to 1 (6 amber / 3 green; two `HEALTH` audit entries are the trace); the weekly client pack shows a Health section with the "over-commitment is not counted — staffing is not shown to clients" disclosure. **Two things named rather than glossed**: (1) the quick filter's autofocus could not be observed here because the automation browser reports `(pointer: coarse)`, which is exactly the touch case the hook withholds focus for — a direct `.focus()` sticks, nothing is `inert`, so it is verified by construction, not by this environment; (2) the pack's score (1, "No owner 1×1") differs from the internal tile (6) by more than the capacity term — because `clientView` withholds the internal records before the score is computed, which is what `PK4` pins; the plan's step-9 sentence assumed every record was client-visible. **A false alarm, caught and reverted before it landed**: `SLG-001` matched nothing in the quick filter, and a `displayId`-in-the-haystack fix plus scenario `LP1` were written — then found to be wrong (an issue row's `displayId` *is* its id, `lib/tree.ts:123`; the miss was the resting client facet scoping the Tree to OAPIL) and reverted uncommitted. Originally: designed and planned 10 Sep. Design: `docs/plans/2026-09-10-fno-page-grammar-design.md`; plan: `2026-09-10-fno-page-grammar-plan.md` (9 steps, 6 commits). Nishant's request, grounded in Microsoft's own F&O pattern docs (List Page, Details, Workspace, FactBox, General Form Guidelines). Three decisions taken in review: page grammar not the Fluent skin; the 7 Sep dashboards decision reopened (count tiles as the home workspace — never built, so no shipped code reverses); and an engagement **health score** admitted because clients expect one in the packs — `lib/portfolio.ts`'s refusal ("a sentence about weights nobody can see") kept as the constraint: components are the six existing concern kinds, weights in Configuration, every rendering prints its terms, the client-pack score excludes `capacity` with the pack's `{shown, total}` disclosure, nothing stored. The shell was already F&O-shaped under other names; the one new element is the FactBox blade. Both this and I29 carry the same guard I27 names — a new model field must be read as `?? DEFAULT` because an existing workspace predates it — which I19's live-verify hit for real with `signatureTemplate`. Highest-risk step named: the `DetailPanel` FastTab rebuild, which touches the two effects whose comments record the past bugs they exist for |
| I31 | **Personal Graph tokens survive a restart — the RAM-only posture reversed** | **Built, 11 Sep, at Nishant's direction** ("there should be a way to connect to your inbox without reconnecting"), when I17's live check found "Your inbox" asking for a reconnect straight after the deploy that shipped it. Design + plan: `docs/plans/2026-09-11-durable-personal-graph-tokens-design.md`. The refresh token — only that — is sealed with AES-256-GCM under a key HKDF-derived from `AXIOMATE_SESSION_SECRET` (`lib/tokenCrypto.ts`, proven by `TOK1`) into a new `PersonalGraphToken` table with RLS; on a cache miss `getPersonalGraphToken` loads and exchanges it, re-seals on rotation, and deletes the row when Entra answers `invalid_grant`. Fail-closed to the old RAM-only behaviour with no database or no usable secret; every database call is inside its own try so no personal-Graph route can turn into a 500. The 31 Aug and 7 Sep designs carry a dated status line naming the reversal. **Acceptance is the next deploy itself**: sign in once, then after the following restart "Your inbox" must list messages with no reconnect. **Sign-in done, 11 Sep** — with a finding on the way: the first hop (Nishant accepting the new `Mail.ReadWrite` consent) ended in the callback's own "This sign-in did not start in this browser" refusal (the App Service log, streamed live) — a state-cookie mismatch, so nothing was stored and the inbox still asked to reconnect; a second, silent hop matched and stored, and the inbox listed 25 messages. The restart half of the acceptance is the deploy that carries this note |

---

## J. Product vision vs. what's actually hosted — the gap survey

*Added 7 September 2026. Compares `docs/strategy/axiomate-vision.md` (12 pillars, 31 Aug) and
`docs/strategy/axiomate-product-blueprint.md` (12 suites / ~100 screens, 6 Sep — self-described as
"not checked against what exists... that reality-check is future work, not done here") against
what is actually in `prisma/schema.prisma` and `components/` today. That reconciliation is run
here. The vision doc already did a pillar-level version of this for pillars 4/6/8/10/12 (§8) — this
extends it to all twelve pillars and, separately, the blueprint's twelve suites, which decompose
the same ambition at a finer grain and surface gaps the pillar framing doesn't (an entire absent
suite reads as a rounding error inside one pillar's "partial" rating).

**Method and its limit.** This is a suite/pillar-level survey, not the screen-by-screen check the
blueprint itself calls for (roughly 100 screens, IDs like DLV-014) — that remains separate, larger,
unstarted work. What's below is accurate about which *suites* have real structure behind them and
which are named only in the blueprint; it is not a claim about any individual screen ID.

### What's substantially real (structural, not AI-dependent)

| Pillar / Suite | Evidence | Gap against the stated target |
|---|---|---|
| **Delivery** (pillar 4, Suite 05) | The core of the built product: `HierarchyNode`/`Issue`/`IssueActivity`/`IssueDependency`, `TreeGrid`, `GanttChart`, `BoardView`, `CalendarView` — List/Kanban/Gantt/Calendar views all real | No first-class **Deliverable** entity distinct from Issue (DLV-013's Quality Criteria/Review/Approval/Version/Outcome shape isn't modelled — `Evidence`/`DocumentReview` cover adjacent ground, not this). No cross-level Action→Deliverable→Milestone→Engagement dependency visualization (DLV-015) beyond what Gantt already shows at the Issue level |
| **Governance** (pillar 12, Suite 12) | One of the strongest areas, not one of the weakest as the blueprint's screen count might suggest: `lib/access.ts`'s granular RBAC (34+ permissions), full audit trail (`ScheduleAudit`), row-level security across all 39 tenant-scoped tables (verified 7 Sep, see section F above) | No unified **Audit Center** screen (GOV-007) — audit data exists, browsable per-record via History tabs, not as a firm-wide searchable log. No **Organization Workspace** (Business Units/Practices/Locations, GOV-002) beyond what `HierarchyNode`'s generic tiers already express. No AI/Agent Governance policy screens (GOV-005/006) — the `Agent` entity has `autonomy`/`requireApproval` fields, a real start, not a configurable policy surface |
| **Workforce Intelligence** (pillar 10, People Suite) | `PersonRate`, `PersonSkill`, `Allocation`, `CapacityPanel.tsx`, effective-dated working patterns | No career-level progression (PPL-003), no Capability Matrix as a demand/supply/gap view (skills are recorded, not matched against demand), no AI staffing recommendation (PPL-006 — named AI-blocked below) |
| **Work Management** (pillar 4 partial, Command Suite partial) | `MyWeek.tsx`/`MyWorkPanel.tsx` cover CMD-006's Today/Upcoming/Overdue/Blocked/Waiting shape closely | No persona-differentiated command centers (CMD-001 through CMD-005 — Associate/Consultant/Senior Consultant/Principal/Engagement Leader each seeing a different cockpit). Today the same screens render for everyone; career level exists in the role model but doesn't yet drive what's shown |
| **Risk Intelligence** (pillar 8, partial Intelligence Suite) | `lib/watch.ts`'s six conditions (`overdue`, `atRisk`, `dueSoon`, `stale`, `planImpossible`, `sowOverConsumed`) — real, structural, automated | Not surfaced as a unified "Intelligence Center" (INT-001) with the Ask/Analyze/Predict/Recommend/Simulate/Execute action set — it's a background pass feeding notifications, not an explorable surface |
| **Business Operations** (pillar 11, Finance Suite partial, Engagement Suite partial) | `Sow`/`ChangeRequest`/`Milestone`/`ScopeItem`, `CommercialPanel.tsx`, `FinanceReportView.tsx`; `isBillable`/`milestonePosition` (`lib/milestone.ts`) — genuinely sophisticated billing-eligibility logic | Invoicing itself designed today, not built (I2c above). No Profitability-by-resource/practice rollup (FIN-003). No Engagement Decision Center (ENG-007) or governance calendar (ENG-005) as distinct concepts — `Meeting` is generic, not engagement-governance-specific |
| **Automation** (pillar 7, Suite 11) | Real, more than the blueprint's framing suggests is typical for a v1: 4 enabled automation rules (`issue.created`, `issue.overdue`, `sow.overConsumed`, `issue.owner`), an `Agent` registry with autonomy/approval settings, a `Workflow` concept (`ConfigWorkspace.tsx`'s Workflows section) | Workflows section is read-only — no Workflow Studio (AUT-002) to author a Trigger→Condition→Decision→Action chain from the UI. No Agent Studio (AUT-004) to configure a new agent's objective/knowledge/tools. No distinct Execution History (AUT-006) — folded into the general `ScheduleAudit` |

### What's absent — not "partial," genuinely not started

| Suite | What the blueprint describes | What exists today |
|---|---|---|
| **Growth Suite** (pipeline, opportunities, proposals, contracts as a sales motion) | GRT-001 through GRT-007 — pipeline, qualification, solution/estimation, proposal, contract, expansion intelligence | Nothing. No `Opportunity`, `Lead`, `Proposal`, or pre-`Engagement` pipeline concept anywhere in `prisma/schema.prisma`. An engagement exists only once it's already real |
| **Client Suite** (client as a first-class entity with health, relationship, satisfaction) | CLT-001 through CLT-005 | `Client` exists only as a `HierarchyNode` tier — a place in the tree, not an entity with stakeholder sentiment, relationship strength, satisfaction scoring, or a 360° rollup across engagements |
| **Knowledge Suite** (solutions, architecture patterns, decisions, lessons, accelerators as a searchable library) | KNW-001 through KNW-007 | `Document`/`DocumentReview` are generic file storage. No Solution Library, Architecture Library, Decision Library, or Lessons Learned structure (Situation→Action→Outcome→Lesson) |
| **Application Suite** (the client's own technology landscape — D365, Salesforce, M365, integrations, health) | Flagged in the source pitch itself as *"a candidate flagship capability"* — APP-001 through APP-007 | **Built, 7 Sep** — see `docs/plans/2026-09-07-application-suite-design.md`: `Application`/`IntegrationLink` models (migration `20260907000001_application_suite`, forced RLS per the standing discipline), health as named concerns (`lib/portfolio.ts`'s own doctrine — no score) rather than the blueprint's Healthy/Watch/At Risk/Critical enum, an `Applications` view in the sidebar with add/edit and integration recording, `Issue.applicationId` additive to `module`. Six scenarios (APP1–APP6, all PASS) drive the real reducer. Scoped to the record-keeping half only — no live telemetry, no AI consequence-chain reasoning, neither backable honestly yet. Migration not run against a live database from this environment — verify on first real deploy |

### AI-blocked — structurally ready, waiting on the one thing vision.md already named

Pillar 3 (Work Intelligence — detecting commitments/decisions/requirements from unstructured
text), the generative half of pillar 1 (Work Capture), most of the Intelligence Suite's
predictive/scenario capability (INT-004/005/006), and AI Command (AXS-003) all need an LLM that
can *read and act*, not narrate. `/api/assist` today is read-only prose over already-computed
figures (`lib/assist.ts`), explicitly walled off from writing anything. `axiomate-vision.md` §5
already names the underlying blocker (zero Anthropic API credits, task #113) — unchanged as of
7 Sep. Nothing in this survey changes that finding; it's restated here because it explains why
three of twelve suites (Growth, Knowledge, most of Intelligence) can't fully close even once
built — their differentiated value is generative, not just structural.

### What this changes about "next," updated from vision.md §8

Vision.md named pillar 9 (Organizational Memory) and the remainder of pillar 11 (Business
Operations) as the strongest non-AI-blocked candidates on 31 Aug. Business Operations moved
today — invoicing is designed (I2c). What the suite-level view adds that the pillar view didn't
surface as sharply: **Client Suite** and **Application Suite** are both zero-built, both
structural (no AI needed for a client health rollup or an application landscape record), and
**Application Suite** in particular sits directly on Axiocloud's own delivery specialism —
arguably a stronger near-term candidate than either pillar 9 or the Growth Suite, which is also
zero-built but whose value (pipeline, proposals) is further from what this codebase's actual
users — internal delivery — do day to day.

---

## Current state, for reference

*Updated 10 September 2026 — the 17 August figures below were three weeks stale (G5 was
unactionable off them). Register and directory counts are what the live screens showed today;
where a figure could not be directly reconciled to the old format without guessing, it is
reported as observed rather than forced to match, per this doc's own operating principles.*

    production   https://axiomate-tms.azurewebsites.net   healthy, database connected
                 confirmed live 10 Sep — see the incident note below
                 B1 Basic, Central India, NODE|22-lts, 40 migrations applied

    register     Analytics, by client: OAPIL 91 open (46 High), SLG 31 open (11 High)
                 Tree view total (both clients, completed hidden): 135 issues
                 not reconciled to a single "all issues including internal" figure this
                 pass — the 216 the 17 Aug figure quoted mixed several filters; a clean
                 recount needs one screen with every filter cleared, not attempted here
    directory    25 active (People screen's own count; some are Departed and excluded,
                 per I22's Active/Departed status)
    suite        260 scenarios — 241 PASS, 17 PARTIAL, 1 NOT IMPLEMENTED, 1 NOT TESTABLE
                 0 P0 gaps (3 P0-severity scenarios, all PASS); 3 P1 gaps — D, ST2b, AI1/X
                 (see G5)
    proofs       persistence 74/74, attribution 3/3, tenancy PASS (136 calls, 39 row mappers)
    access       47 permissions (`lib/access.ts`'s `PERMISSIONS` array) — coverage against
                 stored roles not reverified this pass

**Incident note, 10 Sep.** Two routine deploys today (`d02a905`, `f61824f`) each triggered
App Service's normal staging-slot-swap restart cycle — the container legitimately stopped and
restarted a few times over about 20 seconds per deploy. Nishant reported "still see problem"
mid-window; investigated live (App Service logs, `/api/health`, a direct check of the session-cookie
mechanism) and confirmed: no ongoing crash loop (zero restarts in the 11+ minutes after the second
deploy finished), sessions are signed with a stable `AXIOMATE_SESSION_SECRET` app setting and
survive a container restart, and his own session did survive both deploys with no re-auth needed.
Confirmed resolved by Nishant directly. Worth noting for later: a docs-only commit (`f61824f`)
still runs the full deploy pipeline and its restart cycle — nothing to fix now, but a candidate for
a path-filtered CI trigger if docs-only changes become frequent.
